# Word Cache

To build a cache of words, run 

    pip install -r requirements.txt
    python build_word_cache.py

You should first set its internal variables to 

BUILD_SYNONYMS   = True
BUILD_LEMMAS     = True
BUILD_JOIN       = True